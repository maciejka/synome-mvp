# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

[Brief description of what this project does and its main purpose]

## Development Commands

### Setup
```bash
# [Add installation/setup commands]
```

### Running the Application
```bash
# [Add command to start the dev server or run the application]
```

### Testing
```bash
# Run all tests
# [Add test command]

# Run a single test file
# [Add command to run specific test]

# Run tests in watch mode
# [Add watch mode command if applicable]
```

### Build
```bash
# [Add build command]
```

### Linting
```bash
# [Add lint command]
```

## Architecture

### High-Level Structure

[Describe the overall architecture - e.g., "This is a React frontend with a FastAPI backend" or "Monorepo with multiple packages" or "Microservices architecture with X, Y, Z services"]

### Key Directories

[List important directories and their purposes, e.g.:
- `src/` - Main application code
- `lib/` - Shared utilities
- `api/` - Backend API endpoints
]

### Important Patterns

[Describe any critical architectural patterns or conventions that aren't obvious, e.g.:
- How state management works
- How database migrations are handled
- How API routes are structured
- Any unusual build or deployment patterns
]

## Configuration

[Note any important configuration files or environment variables that need to be set up]
